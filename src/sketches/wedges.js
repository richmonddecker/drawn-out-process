import { Driftable } from "./utility/randomness";

const sketch = (p) => {

  class Wedge {
    constructor(radius, angle, orientation, center) {
      this.radius = radius
      this.angle = angle
      this.orientation = orientation
      this.center = center
    }
  }

  //TODO: HAVE A CENTER CIRCLE MADE UP OF WEDGES THAT FLY IN AND OUT FROM OFFSCREEN

  

};

export default sketch;
